apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextflow-workdir
  namespace: nextflow-tests
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
